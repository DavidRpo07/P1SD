apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: groupsapp-auth-service:latest
          ports:
            - containerPort: 50051
              name: grpc
          env:
            - name: AUTH_GRPC_URL
              value: "0.0.0.0:50051"
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_HOST
            - name: POSTGRES_PORT
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_PORT
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_USER
            - name: POSTGRES_SSL
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_SSL
            - name: POSTGRES_SSL_REJECT_UNAUTHORIZED
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_SSL_REJECT_UNAUTHORIZED
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: groupsapp-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: "groupsapp_auth"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: groupsapp-secrets
                  key: JWT_SECRET
          readinessProbe:
            tcpSocket:
              port: 50051
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 50051
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
