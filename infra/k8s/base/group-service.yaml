apiVersion: apps/v1
kind: Deployment
metadata:
  name: group-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: group-service
  template:
    metadata:
      labels:
        app: group-service
    spec:
      containers:
        - name: group-service
          image: groupsapp-group-service:latest
          ports:
            - containerPort: 50052
              name: grpc
          env:
            - name: GROUP_GRPC_URL
              value: "0.0.0.0:50052"
            - name: AUTH_SERVICE_GRPC_URL
              value: "auth-service:50051"
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_HOST
            - name: POSTGRES_PORT
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_PORT
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_USER
            - name: POSTGRES_SSL
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_SSL
            - name: POSTGRES_SSL_REJECT_UNAUTHORIZED
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: POSTGRES_SSL_REJECT_UNAUTHORIZED
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: groupsapp-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: "groupsapp_group"
          readinessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 50052
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: group-service
spec:
  selector:
    app: group-service
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
